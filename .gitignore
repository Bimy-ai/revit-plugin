# ─── .NET build artifacts ────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.dll
*.pdb
*.exe
*.deps.json
*.runtimeconfig.json

# NuGet
*.nupkg
*.snupkg
packages/
.nuget/

# ─── IDE / editor ────────────────────────────────────────────────────────────
.vs/
.vscode/
.idea/
*.swp
*~

# Rider
_ReSharper.Caches/
*.DotSettings.user

# ─── OS ──────────────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store

# ─── Agent / tooling state ───────────────────────────────────────────────────
.claude/

# ─── Plugin runtime state (written next to the DLL, usually not in-tree) ─────
# Current builds keep all of this in %LOCALAPPDATA%\BIMy; pre-1.1 builds wrote
# it next to the DLL, which lands in-tree during a -Fast dev loop.
RevitWallsPlugin.session.json
RevitWallsPlugin.lasturl.txt
RevitWallsPlugin.lastProjectId.*.txt
*.log

# ─── .NET publish / test output ──────────────────────────────────────────────
publish/
TestResults/
*.coverage
coverage/

# ─── Installer build output (Inno Setup) ─────────────────────────────────────
installer/Output/
*.msi
Setup.exe

# ─── Ad-hoc scratch files ────────────────────────────────────────────────────
_export_inspect.json
_uobj.json
